;; TODO undone
;; okay, so the actual problem I'm having is that
;; unlike in C languages, I can't just bind a variable
;; to something in an if statement and then have it return.
;; bahhhh

(defn ensure-nonempty-in-str []
  (loop []
    (if (<= ))))
